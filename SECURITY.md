# Security

This dashboard is designed for localhost or trusted LAN use only.

It can read and refresh OAuth credentials. Do not expose it to the public internet. Do not commit real `auth.json`, `config.yaml`, cache files, or statusline files.

Report security issues privately to the repository owner rather than opening a public issue with credential details.
